name: lwd_2050
description: Professional LWD Testing Application
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.35.5
  fl_chart: ^0.68.0
  csv: ^5.1.1
  permission_handler: ^11.3.1
  intl: ^0.19.0
  geolocator: ^12.0.0
  shared_preferences: ^2.3.2
  share_plus: ^10.0.2
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true